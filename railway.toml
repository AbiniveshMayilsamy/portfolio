[build]
builder = "nixpacks"
buildCommand = "npm install --prefix client && npm run build --prefix client"

[deploy]
startCommand = "npm install --prefix server && node server/index.js"
restartPolicyType = "on_failure"
